use bevy::prelude::*;
use bevy::math::bounding::{Aabb2d, BoundingVolume, IntersectsVolume};
use bevy::window::{PresentMode, WindowMode};

use crate::ball::*;
use crate::paddles::*;
use crate::startup::*;


